using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;

public class HealthManager : MonoBehaviour
{

    /// <summary>
    //This is temporarily codw based on a tutorial. We will want to intergrate is differently later on, but for now it can maybe be useful.
    //Tutorial found here: https://www.youtube.com/watch?v=0tDPxNB2JNs
    /// </summary>

    public Image healthBar;
    public GameObject player;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
